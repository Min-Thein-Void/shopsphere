<RouterLink :to="{ name: 'Detail', params: { id: product.id } }">
    <div
      class="max-w-[350px] h-[400px] hover:scale-105 transition duration-300 ease-out rounded-lg overflow-hidden shadow-md bg-gray-900 border border-white/30 p-4">

      <!-- Bind image correctly -->
      <img class="w-full h-48 object-cover rounded mb-4 border border-white/20" :src="product.image ? imageUrl : 'public/favicon.ico'" :alt="product?.name" />

      <h3 class="text-lg font-semibold mb-2 text-orange-400">
        {{ product?.name }}
      </h3>

      <p class="text-orange-300 mb-2">
        ${{ product?.price }}
      </p>

      <p class="text-sm text-gray-300 mb-4">
        {{ product?.description }}
      </p>

      <p class="text-sm font-medium mb-4" :class="product?.stock > 0 ? 'text-green-400' : 'text-red-400'">
        {{ product?.stock > 0 ? `In stock: ${product.stock}` : 'Out of stock' }}
      </p>
    </div>
  </RouterLink>

